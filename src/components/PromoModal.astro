---
---

<div id="promo-modal" class="fixed inset-0 z-50 bg-black/60 backdrop-blur-sm flex items-center justify-center px-4">
  <div id="promo-box" class="bg-white max-w-lg w-full p-6 rounded shadow-lg relative text-primary-900">
    <button
      id="promo-close"
      class="absolute top-2 right-3 text-xl text-gray-600 hover:text-red-500"
      aria-label="Close promo"
    >
      ×
    </button>

    <h2 class="text-2xl font-bold mb-4 text-center">🚀 <span class="current-month"></span> Flight Specials</h2>

    <div class="mb-6">
      <h3 class="text-xl font-semibold text-primary-800">🎉 $99 Discovery Flight – “Try the Skies”</h3>
      <p class="mt-1 text-base">
        Ever wondered what it’s like to fly a plane? Now’s your chance.
        All <span class="current-month"></span> long, first-time Discovery Flights are just <strong>$99</strong> (tax included).
        <br />
        <span class="text-sm text-gray-600 italic">Limit one per person. FAA eligibility required.</span>
      </p>
    </div>

    <div>
      <h3 class="text-xl font-semibold text-primary-800">💡 “Fly Now, Train Later” Credit</h3>
      <p class="mt-1 text-base">
        Take the controls, fall in love with flight, and we’ll put <strong>$75</strong> of your Discovery Flight toward your first lesson.
        Book a $150 Discovery Flight and enroll in <span class="current-month"></span> or <span class="next-month"></span> to claim your credit!
      </p>
    </div>

    <div class="mt-6 flex justify-center">
      <a href="/discovery-flight" class="btn-primary">Book Now</a>
    </div>

    <p class="mt-3 text-xs text-center text-gray-500">
      Speedway Flight Training – Safety first. Fun always.
    </p>
  </div>
</div>

<script>
  const modal = document.getElementById("promo-modal");
  const box = document.getElementById("promo-box");
  const closeBtn = document.getElementById("promo-close");

  // Disable scroll
  document.body.classList.add("overflow-hidden");

  const closeModal = () => {
    modal?.remove();
    document.body.classList.remove("overflow-hidden");
  };

  // Close on X
  closeBtn?.addEventListener("click", closeModal);

  // Close on outside click
  modal?.addEventListener("click", (e) => {
    if (!box.contains(e.target as Node)) closeModal();
  });

  // Close on Escape
  document.addEventListener("keydown", (e) => {
    if (e.key === "Escape") closeModal();
  });
</script>
<script>
  document.addEventListener("DOMContentLoaded", () => {
    const now = new Date();
    const currentMonth = now.toLocaleString("default", { month: "long" });

    const next = new Date(now);
    next.setMonth(next.getMonth() + 1);
    const nextMonth = next.toLocaleString("default", { month: "long" });

    document.querySelectorAll(".current-month")
      .forEach(el => el.textContent = currentMonth);

    document.querySelectorAll(".next-month")
      .forEach(el => el.textContent = nextMonth);
  });
</script>
