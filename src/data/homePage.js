import {
  COMPANY_NAME,
  SITE_DESCRIPTION,
  KEYWORDS,
} from "../consts";

const homePage = {
  data: {
    //SEO Header Stuff
    pageTitle: `Atlanta Hampton Flight School | ${COMPANY_NAME}`,
    pageDescription:
      `${SITE_DESCRIPTION}`,
    pageKeywords:
      `${KEYWORDS}`,
  },
};
export default homePage;
