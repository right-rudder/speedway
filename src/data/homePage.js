import {
  COMPANY_NAME,
  SITE_DESCRIPTION,
  KEYWORDS,
} from "../consts";

const homePage = {
  data: {
    //SEO Header Stuff
    pageTitle: `Hampton Los Angeles Flight School for Pilot Training | ${COMPANY_NAME}`,
    pageDescription:
      `${SITE_DESCRIPTION}`,
    pageKeywords:
      `${KEYWORDS}`,
  },
};
export default homePage;
